name: Deploy
  uses: peaceiris/actions-gh-pages@v3
  with:
    github_token: ${{ secrets.MY_TOKEN }}
    publish_dir: ./public
    user_name: 'github-actions'
    user_email: 'child-eating-tech@noreply.github.com'
